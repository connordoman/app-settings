Use the latest idiomatic Go APIs & patterns for this project.

For SQL, use all utilities at your disposal to keep code readable, including reusable functions for common patterns, small, well-described migrations, and snappy queries.

Use `tern` for migrations.

Utilize a `justfile` for maintainability and reproducability.
